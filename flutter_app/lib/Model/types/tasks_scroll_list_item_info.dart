class TasksScrollListItemInfo {
  final int id;
  final String title;
  final bool done;
  final DateTime? limit;
  final int depth;

  TasksScrollListItemInfo(
      this.id, this.title, this.done, this.limit, this.depth);
}
